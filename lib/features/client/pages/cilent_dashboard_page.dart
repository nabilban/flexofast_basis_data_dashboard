import 'package:flutter/material.dart';

class CilentDashboardPage extends StatelessWidget {
  const CilentDashboardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
